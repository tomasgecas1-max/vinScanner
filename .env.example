# CarsXE – automobilio specifikacijos pagal VIN (https://www.carsxe.com/)
CARSXE_API_KEY=your_carsxe_api_key

# One Auto API (VIN patikra – Ezyvin, Cartell, CarGuide ir kt.)
VIN_API_KEY=your_one_auto_api_key

# Test režimui: TIK 4 One Auto endpointai (VIN Decoder, Salvage, OE Europe, OE Global)
# VITE_TEST_ONE_AUTO_ENDPOINTS=true

# Stripe (mokėjimai)
# Publishable key – naudojamas frontende (React). Saugus viešai (Stripe rekomenduoja).
# Vercel neperspės jei pavadinime nėra "KEY" – naudokite VITE_STRIPE_PUBLISHABLE.
VITE_STRIPE_PUBLISHABLE=pk_test_xxxxx

# Secret key – NAUDOTI TIK BACKENDE (Vercel Function, Node). Niekada nefrontende, nekomituoti.
STRIPE_SECRET_KEY=sk_test_xxxxx

# Hostinger SMTP (el. pašto siuntimas klientams po mokėjimo)
# Hostinger: smtp.hostinger.com, port 587
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=info@vinscanner.eu
SMTP_PASS=your_email_password
SMTP_FROM=info@vinscanner.eu

# Firebase Admin (VIN ataskaitų talpykla – apeina API jei ataskaita < 3 mėn.)
# Firebase Console → Project Settings → Service accounts → Generate new private key
# JSON įrašyk kaip vieną eilutę arba base64 ir įkelt į Vercel env
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account",...}
